# MinecraftAlex — Java Minecraft-like на jMonkeyEngine

MinecraftAlex — это учебный проект по созданию собственного Minecraft-подобного движка на Java с использованием jMonkeyEngine.  

---

## На данный момент реализованы базовые механики:
### Генерация мира чанками 16×16×16
- Бесконечная генерация мира по мере перемещения игрока.  
- Загрузка чанков вокруг игрока в радиусе `VIEW_RADIUS_CHUNKS`.  
- Выгрузка далёких чанков для оптимизации.  
- Генерация рельефа на основе **Perlin Noise** (горы, низины, холмы).  
- Каждый чанк — независимый контейнер блоков.

### Типы блоков и материалы
- Реализованы блоки:  
  `AIR`, `GRASS`, `DIRT`, `STONE`, `WATER`.  
- Материалы генерируются автоматически (Lighting.j3md).  
- Поддержка солидных и прозрачных блоков.

### Генерация воды
- Автоматическое заполнение низин водой.  
- Фиксированный уровень воды (`waterSurfaceY`), формирующий озёра и моря.  
- Вода отрисовывается как отдельный тип блока.

### Ломание и установка блоков
- Raycast определяет блок, на который смотрит игрок.  
- ЛКМ — уничтожение блока.  
- ПКМ — установка блока в соседнюю клетку.  
- Защита от «застройки» игрока.  
- После каждого изменения чанк автоматически пересобирается:
  - mesh заново строится,
  - пересоздаётся `CollisionShape`,
  - обновляется физическая модель.

### Подсветка выбранного блока
- Прозрачный выделяющий куб, точно совпадающий с координатами блока.  
- Обновляется каждый кадр через raycast.

### Физическая модель игрока (Bullet Physics)
- Капсульный контроллер (`CapsuleCollisionShape`).  
- Гравитация, прыжок, падение.  
- Корректная физическая коллизия с блоками.  
- Камера прикреплена к персонажу (режим от первого лица).

### Управление
- **W A S D** — движение  
- **SPACE** — прыжок  
- **ЛКМ** — сломать блок  
- **ПКМ** — поставить блок  
- **Мышь** — обзор

### Освещение и визуализация
- Направленный свет (sunlight).  
- Ambient-освещение.  
- Небесный градиент (sky-color).  
- Отрисовка блоков через статические Mesh-кубы.

---

## 🛠️ Технологии

- Java 17  
- Maven  
- jMonkeyEngine 3.3.2-stable  
- LWJGL3  
- Bullet Physics  

---

## 🚀 Установка и сборка

Требования:

- Java 17+
- Maven 3+

Сборка проекта:

```bash
mvn clean package
